# 연금플러스 MVP 코딩 에이전트 지침

## 프로젝트 목표

4일 안에 기능1 이관 현황판과 기능2 연금 외 수령(중도인출) 시뮬레이터가 작동하는 모바일 웹 MVP를 만든다. 실제 금융 서비스가 아니라 사용자 가설 검증용이며 모든 계좌·이관 데이터는 mock이다.

## 작업 전 필수 확인

1. `docs/team/start/개발전_준비.md`를 읽는다.
2. `docs/team/development/개발환경_협업가이드.md`를 읽는다.
3. UI 작업이면 `docs/team/design/디자인시스템_개발적용가이드.md`를 읽는다.
4. 담당 기능은 `docs/team/product/PRD_원본상세.md`와 `docs/team/product/예상_프로토타입.html`의 관련 부분을 확인한다(구조·흐름·문구 기준).
5. 시각 스타일은 `docs/team/design/design-system/`의 디자인 시스템·모바일 앱 dc.html 두 파일과 이미 구현된 `src/app/HomePage.tsx`, `src/features/onboarding/` 화면을 기준으로 통일한다.
6. 요청받은 담당 폴더와 완료 조건을 확인한다.
7. 관련 파일을 먼저 읽고 기존 구조를 유지한다.

## 기술 기준

- Vite + React + TypeScript
- Tailwind CSS
- React Router
- Vitest
- 로컬 TypeScript mock 데이터
- 시연용 로그인·회원가입 UI와 홈 진입 흐름 포함
- 실제 계정 생성·인증·세션·서버·데이터베이스·실제 금융 API 없음

## 변경 범위

- 요청받은 폴더만 수정한다.
- 공통 설정, 라우터, 패키지, 다른 기능 폴더는 명시적 요청 없이 수정하지 않는다.
- 새 패키지는 명시적 허가 없이 설치하지 않는다.
- 기존 파일 전체를 대체하지 말고 필요한 부분만 수정한다.
- `docs/team/product/예상_프로토타입.html`은 참고 자료이며 직접 수정하지 않는다. 단, 이 파일이
  다른 문서의 라우트 표보다 화면 개수·단계가 더 많으면 프로토타입 쪽을 우선한다 — 표에 없다고
  화면을 빠뜨리지 않는다.

## 구현 규칙

- 모바일 393px 너비(아이폰 17 기준)를 우선한다.
- 계산식은 React 컴포넌트와 분리한다.
- 금액·세율·기준값을 화면에 중복 하드코딩하지 않는다.
- 색상, 간격, 모서리 값을 화면에 직접 하드코딩하지 않고 디자인 토큰을 사용한다.
- `any` 타입을 사용하지 않는다.
- 모든 mock 화면에는 `시연용 데이터`를 표시한다.
- 로그인·회원가입 화면에는 `시연용 화면`을 표시하고 실제 계정이 생성되지 않음을 안내한다.
- 모든 계산 결과에는 `모의계산`과 실제 결과가 달라질 수 있다는 문구를 표시한다.
- 실제 이체·출금·주문이 실행되는 것처럼 오해할 버튼을 만들지 않는다.
- 요청 범위 밖의 기능이나 예외 처리를 임의로 추가하지 않는다.
- 모든 화면에서 뒤로가기(헤더 버튼·브라우저 뒤로가기)가 정상 동작해야 한다. 화면 하나만 따로 동작하면 안 되고, 홈에서 시작해 담당 흐름의 끝까지 막히거나 끊기지 않고 자연스럽게 이어져야 한다.

## 완료 조건

- 요청된 사용자 흐름이 브라우저에서 작동한다.
- `npm run build`가 성공한다.
- 계산 변경 시 `npm run test`가 성공한다.
- 변경 파일과 수동 확인 방법을 보고한다.
- 완료하지 못한 항목과 남은 위험을 숨기지 않고 보고한다.
